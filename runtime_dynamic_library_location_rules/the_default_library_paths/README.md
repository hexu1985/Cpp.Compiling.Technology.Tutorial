### 默认库路径（/lib 与 /usr/lib）

路径 `/lib` 和 `/usr/lib` 是 Linux 操作系统存放动态库的两个默认位置。
设计供超级用户使用和/或面向所有用户的第三方程序，通常会将其动态库部署到这两个目录之一。

需要注意的是，路径 `/usr/local/lib` 并不属于此类别。当然，您仍然可以通过前面介绍的任意机制将其添加到优先级列表中。


#### 参考资料:
《高级C/C++编译技术》: 7.3.1 Linux运行时动态库文件的定位规则

